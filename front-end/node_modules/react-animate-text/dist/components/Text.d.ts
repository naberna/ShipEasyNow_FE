import * as React from 'react';
import { Subscription } from 'rxjs';
import TextAnimation from './TextAnimation';
export interface Props {
    children: string;
    isLastOne?: boolean;
}
interface State {
    currentText: string;
}
export default class Text extends React.Component<Props, State> {
    static contextType: React.Context<Partial<React.ComponentPropsWithoutRef<typeof TextAnimation>>>;
    static defaultProps: {
        charInterval: number;
        type: string;
    };
    subscription: Subscription;
    state: {
        currentText: string;
    };
    componentDidMount(): void;
    componentWillUnmount(): void;
    getCurrentText: (currentCharIndex: number) => string;
    onNextChar: () => any;
    render(): string;
}
export {};
