import * as React from 'react';
import { Animation } from '../types';
interface Props {
    children: React.ReactNode;
    charInterval: number;
    animation?: Animation;
    onComplete?: () => void;
}
interface State {
    currentText: number;
}
declare class Core extends React.Component<Props, State> {
    static defaultProps: {
        animation: string;
        charInterval: number;
    };
    state: {
        currentText: number;
    };
    increaseCurrentText: () => void;
    render(): React.ReactNode;
}
export default Core;
